name: excel_file
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^10.3.3
  excel: ^4.0.6
  intl: ^0.20.2
#  url_launcher:
  open_file: ^3.5.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
flutter_intl:
  enabled: true
